# 🚀 ابدأ من هنا - رفع المشروع على GitHub

## الطريقة الأسهل والأسرع:

### 1. افتح PowerShell في مجلد المشروع

### 2. قم بتشغيل السكريبت:

```powershell
.\رفع_تلقائي.ps1
```

### 3. أدخل معلوماتك:
- اسم المستخدم على GitHub
- اسم المستودع (مثال: `car-rental-pro`)

### 4. أنشئ المستودع على GitHub:
- اذهب إلى: https://github.com/new
- أدخل نفس اسم المستودع
- **لا تضع علامة على "Initialize with README"**
- انقر "Create repository"

### 5. ارفع المشروع:
```powershell
git push -u origin main
```

---

## أو استخدم الأوامر يدوياً:

```powershell
# 1. تهيئة Git
git init

# 2. إضافة الملفات
git add .

# 3. Commit
git commit -m "Initial commit: Car Rental PRO Dashboard"

# 4. إضافة رابط GitHub (استبدل YOUR_USERNAME و REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# 5. تغيير اسم الفرع
git branch -M main

# 6. رفع المشروع
git push -u origin main
```

---

## ⚠️ ملاحظات مهمة:

1. **يجب إنشاء المستودع على GitHub أولاً** قبل `git push`
2. **سيطلب منك اسم المستخدم وكلمة المرور** عند الرفع
3. **إذا استخدمت 2FA**، استخدم Personal Access Token بدلاً من كلمة المرور

---

## 🔐 إنشاء Personal Access Token:

1. اذهب إلى: https://github.com/settings/tokens
2. انقر "Generate new token (classic)"
3. اختر الصلاحيات: `repo` (كامل)
4. انسخ الـ Token
5. استخدمه ككلمة مرور عند `git push`

---

**بعد الرفع، سيكون المشروع متاحاً على GitHub!** 🎉

