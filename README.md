# Car Rental PRO — Admin Dashboard

مشروع لوحة تحكم احترافية لإدارة تأجير السيارات مع دعم Firebase كامل وواجهة عربية.

## المميزات الرئيسية
✅ إدارة السيارات والإيجارات  
✅ تتبع الإيرادات والنفقات  
✅ تقارير شهرية مع رسوم بيانية  
✅ إدارة المستخدمين والأدوار  
✅ النسخ الاحتياطية التلقائية  
✅ دعم عربي كامل مع RTL  
✅ مظهر ليل/نهار  
✅ التكامل مع Firebase Auth و Firestore  

## التكنولوجيا المستخدمة
- **Frontend**: HTML5, CSS3, JavaScript (ES6+)
- **Build Tool**: Vite
- **Backend**: Firebase (Auth + Firestore)
- **Charts**: Chart.js
- **Linting**: ESLint + Prettier
- **CI/CD**: GitHub Actions
- **Hosting**: Firebase Hosting

## البدء السريع

### 1. التثبيت
```bash
npm install
```

### 2. التطوير المحلي
```bash
npm run dev
```
الموقع سيفتح تلقائياً على http://localhost:5173

### 3. البناء والنشر
```bash
npm run build
firebase deploy
```

## إعدادات Firebase

### 1. إنشاء حساب Firebase
1. اذهب إلى https://firebase.google.com
2. أنشئ مشروع جديد
3. اختر `cars-9b2bd` أو أي معرّف آخر

### 2. تكوين المصادقة
1. في Firebase Console → Authentication
2. اختر "Sign-in method" → "Email/Password"
3. فعّل "Email/Password" و "Anonymous"
4. أنشئ مستخدم تجريبي:
   - Email: `admin@demo`
   - Password: `123456`

### 3. تكوين Firestore
1. في Firebase Console → Firestore Database
2. اختر "Create database"
3. ابدأ بـ "Production mode"
4. اختر منطقة قريبة (مثل `europe-west1`)
5. انسخ قواعد الأمان من `firestore.rules`
6. صقها في Firebase Console → Rules

### 4. إعداد Firebase CLI
```bash
npm install -g firebase-tools
firebase login
firebase init
firebase deploy
```

## الإعدادات (Settings)

### تبديل اللغة
- English / العربية
- يتم حفظ الاختيار في Local Storage

### العملة
- اختر العملة المعروضة (مثال: MRU, USD, EGP)

### Auto-backup
- فعّل/عطّل النسخ الاحتياطية التلقائية
- حدد الفاصل الزمني بالدقائق

### المظهر
- اختر بين Light و Dark theme

## هيكل المشروع
```
cars/
├── src/
│   ├── app.js           # منطق التطبيق الرئيسي
│   ├── i18n.js          # دعم اللغات
│   ├── logger.js        # نظام السجلات
│   └── utils.test.js    # اختبارات الأدوات
├── index.html           # الصفحة الرئيسية
├── package.json         # المتطلبات
├── vite.config.js       # إعدادات Vite
├── firebase.json        # إعدادات Firebase
├── firestore.rules      # قواعد أمان Firestore
└── docs/                # التوثيق
    ├── ARCHITECTURE.md
    ├── DEPLOYMENT.md
    ├── SECURITY.md
    └── CONTRIBUTING.md
```

## الأوامر المتاحة
```bash
npm run dev           # تطوير محلي
npm run build         # بناء الإنتاج
npm run preview       # معاينة البناء
npm run lint          # فحص الكود
npm run lint:fix      # إصلاح تلقائي
npm run format        # تنسيق الكود
```

## نصائح أمان مهمة
⚠️ لا تضع مفاتيح Firebase مباشرة في الكود الأساسي  
⚠️ استخدم `.env.local` (غير مُرفوع) للسرّيات  
⚠️ طبّق قواعد Firestore من `firestore.rules`  
⚠️ لا تخزن كلمات المرور في Firestore  
⚠️ استخدم Firebase Auth للمستخدمين  

## الدعم المتعدد اللغات
التطبيق يدعم:
- ✅ English (LTR)
- ✅ العربية (RTL)

اضف لغات جديدة في `src/i18n.js`:
```javascript
ar: {
  key: 'ترجمة عربية',
  // ...
}
```

## استكشاف الأخطاء
### الصفحة بيضاء
- افتح Developer Tools (F12)
- تحقق من Console للأخطاء
- تأكد أن Firebase مهيأ صحيحاً

### لا تظهر البيانات
- تحقق من قواعد Firestore
- تأكد من تسجيل الدخول
- تحقق من Network في DevTools

### خطأ في النسخ الاحتياطية
- تأكد من تفعيل "Auto-backup"
- تحقق من صلاحيات Firestore

## المساهمة
1. اقرأ `CONTRIBUTING.md`
2. انسخ المستودع (Fork)
3. أنشئ فرع ميزة (Feature branch)
4. اجعل Commit وادفع (Push)
5. افتح Pull Request

## الترخيص
MIT License - انظر LICENSE لمزيد من التفاصيل

## الدعم
- 📖 اقرأ `ARCHITECTURE.md` للهندسة المعمارية
- 🔒 اقرأ `SECURITY.md` لقضايا الأمان
- 🚀 اقرأ `DEPLOYMENT.md` للنشر

---

تم تطويره باستخدام ❤️ مع Firebase و Vite

